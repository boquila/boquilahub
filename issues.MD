GUI issues

Video

1. Next to the time, I can see, for example:  "person 87%", like: "0:26.70 / 0:26.73 person  ·  87%"
delete that person and percentage thing, it's redundant with the visualization, and a useless compression of the info.
2. If I don't have an AI selected (and the modailiy is not Image) I shouldn't be able to press "Analyze"
3. The "Nearest analyzed frame" text that we see sometimes on hover is cool and great, the problem is that it resizes the size of the hover text in comparison to the hover without that text (like a frame that does actually have an AIOutput) so maybe don't put it at all. I think we should just copy the AIOutputs and fill them up tbh. Instead of having distinctions between fake aitoutputs, nearest frames, whatever. Having this it's too much complexity, delete.

Image 
4. If I don't have an AI selected (and the modailiy is not Image) I shouldn't be able to press "Analyze"

Audio. 
5. next to the filename, central panel on top, I could see: "14 segments" for example. Delete that, and any calculations we do related to that, It's redundant given the visualization.
6. On hover text has: "AI predictions here" delete that text and keep the predictions, redundancy.